let nota01 = Number(prompt('qual é o valor da nota 1°'));
let nota02 = Number(prompt('qual é o valor da nota 2°'));
let nota03 = Number(prompt('qual é o valor da nota 3°'));
let nota04 = Number(prompt('qual é o valor da nota 4°'));

let media = (nota01 + nota02 + nota03 + nota04) / 4;

alert(`A sua nota media é de ${media}`)
